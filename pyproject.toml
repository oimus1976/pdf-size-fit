[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pdf-size-fit"
version = "0.0.0"
description = "Experimental offline routing PoC for fitting oversized PDFs under attachment limits"
requires-python = ">=3.11"
dependencies = [
  "pypdf>=5,<6",
  "pypdfium2>=4,<5",
  "Pillow>=11,<13",
  "tkinterdnd2>=0.4,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "reportlab>=4,<5",
]

[project.scripts]
pdf-size-fit = "pdf_size_fit.fit_cli:main"
pdf-size-fit-diagnose = "pdf_size_fit.cli:main"
pdf-size-fit-image = "pdf_size_fit.image_cli:main"
pdf-size-fit-monochrome = "pdf_size_fit.monochrome_cli:main"

[project.gui-scripts]
pdf-size-fit-gui = "pdf_size_fit.gui:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
